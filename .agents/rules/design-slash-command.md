---
description: Custom slash command for the AI Product Design Pipeline
---

# /design Command

When the user types `/design [Topic/Screen]`, you must immediately activate the `pipeline-master` skill and begin executing the 4-stage product design pipeline for the requested topic or screen, starting with Stage 1 (`product-spec-architect`). Do not ask for permission, just begin generating the Stage 1 output.
